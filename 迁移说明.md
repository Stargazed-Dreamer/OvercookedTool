# OvercookedTool C# 迁移说明

## 1. 需求梳理结论（来自 `overcookedTool\需求文档.txt`）
- 目标：存档包管理 + 多版本转换 + 存档编辑。
- 关键交互：多存档包标签页、拖拽打开、存档列表选择、复制/移动、删除、编辑。
- 关键能力：
  - 自动识别存档平台/格式（OC2 二进制、AYCE JSON、Xbox 命名）。
  - 打开包时自动尝试常见密钥。
  - 复制/移动到目标包时自动做版本转换（OC2 <-> AYCE）。
  - 保留日志与错误捕获，便于开发期排错。

## 2. 旧项目现状（`OvercookSavesEditor`）
- UI：PySide6（结构完整但实现杂糅）。
- 核心：
  - `OvercookedLib.dll` 负责加解密（Python 通过 pythonnet 调 C# DLL）。
  - `transfer.py` 负责 OC2/AYCE 结构转换。
  - `save_manager.py` 中已有“复制到目标包自动转换 + 自动猜密钥”的雏形。
- 问题：Python 与 C# 混用导致维护复杂，文件散落，功能完成度不一致。

## 3. 本次新建 C# 方案（纯 C#）
目录：`overcookedTool\csharp`

- `OvercookedTool.Core`
  - `Crypto/OvercookedCrypto.cs`：从旧 `OvercookedLib` 迁移的加解密与 CRC。
  - `Services/SaveJsonConverter.cs`：迁移 OC2/AYCE 转换逻辑。
  - `Services/SaveFileNameHelper.cs`：统一解析/生成 OC2/AYCE/Xbox 文件名。
  - `Services/KeyDetector.cs`：智能密钥检测（目录名、steam_autocloud accountid、steamid64 推导、Epic 常见值、手工覆盖）。
  - `Services/SavePackageService.cs`：包加载、版本检测、读写 JSON、复制/移动自动转换。
  - `Logging/AppLogger.cs`：日志输出。

- `OvercookedTool.App`（WinForms）
  - `MainForm.cs`：主界面（多标签、打开、刷新、复制/移动自动转换、编辑 JSON、关闭标签）。
  - `PackageTabView.cs`：单包信息面板 + 存档列表 + 手动密钥输入。
  - `JsonEditorForm.cs`：JSON 编辑窗口。
  - `Program.cs`：全局异常捕获 + 日志初始化。

## 4. 本次已验证
- 解决方案可编译：`dotnet build overcookedTool\csharp\OvercookedTool.sln` 通过。
- 烟雾测试（开发过程临时项目）验证：
  - 样例 OC2 包可自动识别密钥并解密读取。
  - 复制到 AYCE 包时可自动完成版本转换并落盘为 `.json`。

## 5. 当前仍可继续增强
- 细粒度的“冲突检查 / 待同步检查”可视化面板。
- 更深入的存档字段可视化编辑（目前是 JSON 编辑器）。
- 历史版本时间线 UI。
- 旧需求中的高级弹窗动效/视觉细节。
